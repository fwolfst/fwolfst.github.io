---
layout: default
---

{{ content }}

%h1 Blog Posts

.post-list
  {%- for post in site.posts -%}
  .post-list-entry
    %h3
      %a.post-link{href: "{{ post.url | relative_url }}"}
        {{ post.title | escape }}
    %span.post-meta
      {{ post.date | date: date_format }}
    {{ post.excerpt }}
  {%- endfor -%}

